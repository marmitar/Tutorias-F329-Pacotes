%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%  CONFIGURAÇÃO  DE  FONTES  %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{pacotes/fontes}


% pacote para opções do tipo "key=value"
\RequirePackage{kvoptions}
\SetupKeyvalOptions{
    family = fontes,
    prefix = fontes@
}
% opção de cor do número da secção
\DeclareStringOption[black]{corlink}[black]
\ProcessKeyvalOptions*

% para reconhecer símbolos do português
\RequirePackage[portuguese]{babel}
% codificação da fonte
\RequirePackage[T1]{fontenc}
\RequirePackage[utf8]{inputenc}
% pacote com fontes matemáticas
\RequirePackage{amsmath, amsfonts, amssymb, amsthm}
% melhores estéticas de fontes
\RequirePackage{microtype}
% controles de sublinhado
\RequirePackage{soulutf8}
% links e referências hipertextual
\RequirePackage[
    backref,
    pagebackref,
    hyperindex,
    colorlinks,
    urlcolor = \fontes@corlink,
    bookmarks,
    bookmarksopen = false,
    breaklinks,
    hidelinks,
]{hyperref}

\setulcolor{\fontes@corlink}
% altera os links para sublinhado
\let\OldHref\href
\renewcommand{\href}[2]{%
    \OldHref{#1}{\ul{#2}}%
}

\endinput
